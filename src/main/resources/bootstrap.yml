spring:
  application:
    name: giu-cloud-config
  cloud:
    config:
      env: develop
      server:
        bootstrap: true
        prefix: /config
        git:
          uri: https://github.com/julianavaca/giu-config
          username: julianavaca
          password: pepeloco123
encrypt:
  key: encryptme
security:
  user:
    password: sarasa
