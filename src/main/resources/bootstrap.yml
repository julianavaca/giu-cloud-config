spring:
  application:
    name: aleph-cloud-config
  cloud:
    config:
      env: dev
      server:
        bootstrap: true
        prefix: /config
        git:
          uri: https://github.com/julianavaca/giu-config
          username: julianavaca
          password: pepeloco123
encrypt:
  key: encryptme
security:
  user:
    password: sarasa
